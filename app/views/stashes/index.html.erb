<h1>Stashes</h1>

<ul>
    <% @stashes.each do |stash| %>
        <li>
            <h4><%= link_to stash.name, stash_path(stash) %></h4>
        </li>
     <% end %>
</ul> 

<h4><%= link_to("Create New Stash", {controller: :stashes, action: :new}) %></h4>
