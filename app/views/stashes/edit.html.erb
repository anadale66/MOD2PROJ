<h1>🧺 Edit Stash</h1>


<%= form_for @stash do |f|%>
<b><%= f.label "Stash Name" %></b>
<%= f.text_field :name, placeholder: "Enter Name" %>
<b><%= f.label "Squirrel" %></b>
<%= f.collection_select :squirrel_id, Squirrel.all, :id, :name %>
<b><%= f.label "Snack" %> </b>
<%= f.collection_select :snack_id, Snack.all, :id, :name %>
<%= f.submit %>
<% end %>


<%= link_to "Back: #{@stash.name}", stash_path(@stash), class: "back-link" %> 